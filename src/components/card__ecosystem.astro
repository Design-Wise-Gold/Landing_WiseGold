---
const { title, description, video } = Astro.props;
---

<div class="flex flex-col lg:flex-row items-left justify-left gap-2 border border-gray-500  w-full lg:w-auto hover:border-2 hover:border-accent transition-all duration-300">
    <div class="flex flex-col p-2 w-full lg:w-auto">
        <span class="text-gray-300  ">{title}</span>
        <p
            class="text-gray-500 lg:mt-3 text-sm  text-left w-full lg:w-2/3"
        >
            {description}
        </p>
    </div>
    <video
        src={video}
        autoplay
        muted
        loop
        playsinline
        class="w-full lg:w-40 h-16 lg:h-full object-cover ">
    </video>
</div>
